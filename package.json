{
  "name": "scorer",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check sports.js && node --test"
  },
  "engines": { "node": ">=20" }
}
